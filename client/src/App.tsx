import ApolloProvider from './ApolloProvider'

import Register from './components/Register'

function App() {
  return (
    <ApolloProvider>
      <Register />
    </ApolloProvider>
  )
}

export default App
